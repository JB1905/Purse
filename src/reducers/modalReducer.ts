import {} from '../actions';

// import {  } from '../interfaces/ModalState';

// import type { ModalActionTypes } from '../types/ModalActionTypes';

const initialState: any = {};

export const modalReducer = (state = initialState, action: any) => {
  switch (action.type) {
    // case TOGGLE_LOCAL_AUTH:
    //   return {
    //     ...state,
    //     localAuth: !state.localAuth,
    //   };

    default:
      return state;
  }
};
